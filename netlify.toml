[build]
  publish = "dist"
  command = "npm run build"

[functions]
  directory = "netlify/functions"

# SPA redirect to make all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
